// maths_page.dart
import 'package:flutter/material.dart';

class MathsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Maths Page'),
      ),
      body: Center(
        child: Text('Maths content goes here'),
      ),
    );
  }
}